import {buildSegments} from './geometry.js';
const PALETTE=['#6c5ce7','#00b894','#0984e3','#fdcb6e','#e17055','#e84393','#00cec9','#a29bfe','#55efc4','#74b9ff','#ffeaa7','#fab1a0'];
function luminance(hex){const h=String(hex).replace('#','');if(!/^[0-9a-f]{6}$/i.test(h))return .5;const rgb=[0,2,4].map(i=>parseInt(h.slice(i,i+2),16)/255).map(v=>v<=.03928?v/12.92:((v+.055)/1.055)**2.4);return .2126*rgb[0]+.7152*rgb[1]+.0722*rgb[2]}
function fitCanvas(canvas,ctx){const dpr=Math.min(3,globalThis.devicePixelRatio||1),rect=canvas.getBoundingClientRect(),w=Math.max(1,Math.floor(rect.width*dpr)),h=Math.max(1,Math.floor(rect.height*dpr));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h}ctx.setTransform(dpr,0,0,dpr,0,0);return{w:rect.width,h:rect.height}}
function trimText(ctx,text,max){let s=String(text);if(ctx.measureText(s).width<=max)return s;while(s.length>1&&ctx.measureText(s+'…').width>max)s=s.slice(0,-1);return s+'…'}
export function drawWheel(ctx,canvas,wheel,rotation=0,options={}){const {w,h}=fitCanvas(canvas,ctx);ctx.clearRect(0,0,w,h);const cx=w/2,cy=h/2,r=Math.max(30,Math.min(w,h)*.43);const entries=(wheel?.entries||[]).filter(e=>String(e.label||'').trim()&&Number(e.weight)>0);const segments=buildSegments(entries);ctx.save();ctx.translate(cx,cy);ctx.rotate(rotation);segments.forEach((s,i)=>{const color=s.entry.color||PALETTE[i%PALETTE.length];ctx.beginPath();ctx.moveTo(0,0);ctx.arc(0,0,r,s.startAngle,s.endAngle);ctx.closePath();ctx.fillStyle=color;ctx.fill();ctx.strokeStyle='rgba(255,255,255,.38)';ctx.lineWidth=1;ctx.stroke();const arc=s.endAngle-s.startAngle;if(segments.length<=80&&arc>.025){ctx.save();ctx.rotate(s.centerAngle);ctx.textAlign='right';ctx.textBaseline='middle';ctx.fillStyle=luminance(color)>.46?'#151515':'#fff';ctx.font=`700 ${Math.max(10,Math.min(18,r*.045))}px system-ui`;ctx.fillText(trimText(ctx,s.entry.label,r*.6),r*.86,0);ctx.restore()}});ctx.restore();ctx.beginPath();ctx.arc(cx,cy,Math.max(20,r*.105),0,Math.PI*2);ctx.fillStyle=options.hubColor||'#111827';ctx.fill();ctx.lineWidth=3;ctx.strokeStyle='rgba(255,255,255,.85)';ctx.stroke();ctx.beginPath();ctx.moveTo(cx,cy-r-12);ctx.lineTo(cx-15,cy-r+18);ctx.lineTo(cx+15,cy-r+18);ctx.closePath();ctx.fillStyle='#fff';ctx.fill();return segments}
